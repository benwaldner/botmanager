# tests/meson.build — placeholder
# Top-level meson.build references subdir('tests'), so this file must exist.
# No test executables are wired up yet; add unit-test targets here as the
# test harness develops.
